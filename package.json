{
  "name": "sdef-reader",
  "version": "1.1.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "pack": "npm install && tsc && npx @anthropic-ai/mcpb pack .",
    "cc-install": "npm run build && claude mcp add sdef-reader -- node $(pwd)/dist/index.js",
    "cc-uninstall": "claude mcp remove sdef-reader"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "fast-xml-parser": "^5.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
